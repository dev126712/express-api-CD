apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  # annotations:
  #   cloud.google.com/neg: '{"ingress": true}'
spec:
  ingressClassName: nginx
  selector:
    app: frontend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
